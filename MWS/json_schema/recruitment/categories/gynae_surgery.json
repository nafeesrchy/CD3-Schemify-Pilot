{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.example.org/cd3-mws-pilot/recruitment/categories/gynae_surgery.json",
  "title": "MWS Recruitment — Gynae Surgery",
  "description": "Hysterectomy, bilateral oophorectomy, and sterilisation history recorded at recruitment.",
  "type": "object",
  "x-variable-group": "gynae_surgery",
  "properties": {
    "RHYST": {
      "title": "Hysterectomy (ever)",
      "oneOf": [
        { "const": 1, "title": "Yes" },
        { "const": 2, "title": "No" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ]
    },
    "RHYSTAG": {
      "title": "Age at hysterectomy",
      "x-unit": "years",
      "x-universe": "Participants who have had a hysterectomy (RHYST=1).",
      "anyOf": [
        { "type": "integer", "minimum": 10, "maximum": 90 },
        { "$ref": "../../common/defs.json#/$defs/not_applicable" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ],
      "$comment": "Source states no valid range; 10-90 is an agent-decided plausibility bound -- D029. not_applicable branch enforced by a mother-file skip pair keyed on RHYST -- D030 (inferred routing, steward-confirmed)."
    },
    "ROOPH": {
      "title": "Bilateral oophorectomy (ever)",
      "oneOf": [
        { "const": 1, "title": "Yes" },
        { "const": 2, "title": "No" },
        { "const": 3, "title": "Not sure" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ]
    },
    "ROOPHAGE": {
      "title": "Age at bilateral oophorectomy",
      "x-unit": "years",
      "x-universe": "Participants who have had a bilateral oophorectomy (ROOPH=1). ROOPH=3 (Not sure) deliberately does NOT also force this not-applicable -- steward-confirmed, D030.",
      "anyOf": [
        { "type": "integer", "minimum": 10, "maximum": 90 },
        { "$ref": "../../common/defs.json#/$defs/not_applicable" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ],
      "$comment": "Source states no valid range; 10-90 is an agent-decided plausibility bound -- D029. not_applicable branch enforced by a mother-file skip pair keyed on ROOPH -- D030 (inferred routing, steward-confirmed)."
    },
    "RSTER": {
      "title": "Sterilised (ever)",
      "oneOf": [
        { "const": 1, "title": "Yes" },
        { "const": 2, "title": "No" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ]
    },
    "RSTERAGE": {
      "title": "Age sterilised",
      "x-unit": "years",
      "x-universe": "Participants who have been sterilised (RSTER=1).",
      "anyOf": [
        { "type": "integer", "minimum": 10, "maximum": 60 },
        { "$ref": "../../common/defs.json#/$defs/not_applicable" },
        { "$ref": "../../common/defs.json#/$defs/missing_numeric" }
      ],
      "$comment": "Source states a valid range of From=10 only (To is blank/unstated). Minimum 10 is source-stated; maximum 60 is an agent-decided plausibility ceiling -- D029. not_applicable branch enforced by a mother-file skip pair keyed on RSTER -- D030 (inferred routing, steward-confirmed)."
    }
  },
  "required": ["RHYST", "RHYSTAG", "ROOPH", "ROOPHAGE", "RSTER", "RSTERAGE"]
}
